import React from 'react';

const StoreItems = ({onRouteChange}) => {

	return(
		<div>
			<h1>Store Inventory</h1>
			<h2>SELECT * FROM store_item;</h2>
			<h3>Put the above results into an HTML formatted table:</h3>
		</div>

		);
}

export default StoreItems;